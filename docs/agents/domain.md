# Domain Docs

This repository uses a single-context domain-documentation layout.

Before exploring product code, read root `CONTEXT.md` when present and relevant ADRs under `docs/adr/`. Use domain terms defined in `CONTEXT.md`; flag conflicts with ADRs rather than silently overriding them. If these documents do not exist yet, proceed without creating them unless domain-modeling work requires them.
